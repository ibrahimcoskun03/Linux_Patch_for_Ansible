
<p>Merhaba,</p>
<p>Aşağıdaki sunucularda Patch &ccedil;alışması tamamlamıştır, kontrolleri sağlayabilirsiniz.</p>


</table>

<table style="border-collapse: collapse; width: 100%;" border="1">
<tbody>
<tr>
  <td style="width: 20%;">Sunucu Hostname</td>
  <td style="width: 20%;">IP adresi</td>
  <td style="width: 20%;">Isletim Sistemi</td>
  <td style="width: 20%;">Isletim Sistemi  Versiyonu</td>
  <td style="width: 20%;">Sunucu Tipi</td>
</tr>
  {% for host in register_success %}
<tr>
  <td style="width: 20%;">{{ hostvars[host].ansible_fqdn }}</td>
  <td style="width: 20%;">{{ hostvars[host].ansible_default_ipv4.address }}</td>
  <td style="width: 20%;">{{ hostvars[host].ansible_distribution }}</td>
  <td style="width: 20%;">{{ hostvars[host].ansible_distribution_version }}</td>
  <td style="width: 20%;">{{ hostvars[host].ansible_product_name }}</td>
</tr>
</tbody>
{% endfor %}
</table>


{% if register_failed %}
<p><strong>NOT:</strong> <span style="color: #000000;"> Aşağıdaki sunucularda Patch çalışması devam etmektedir. Çalışma tamamlandıktan sonra bilgilendirme yapılacaktır.</span></p>

<table style="border-collapse: collapse; width: 100%;" border="1">
<tbody>
<tr>
  <td style="width: 20%;">Sunucu Hostname</td>
  <td style="width: 20%;">IP adresi</td>
  <td style="width: 20%;">Isletim Sistemi</td>
  <td style="width: 20%;">Isletim Sistemi  Versiyonu</td>
  <td style="width: 20%;">Sunucu Tipi</td>
</tr>

{% endif %}

{%  for host in register_failed  %}
 
<tr>
  <td style="width: 20%;">{{ hostvars[host].ansible_fqdn }}</td>
  <td style="width: 20%;">{{ hostvars[host].ansible_default_ipv4.address }}</td>
  <td style="width: 20%;">{{ hostvars[host].ansible_distribution }}</td>
  <td style="width: 20%;">{{ hostvars[host].ansible_distribution_version }}</td>
  <td style="width: 20%;">{{ hostvars[host].ansible_product_name }}</td>
</tr>

{% endfor %}
</tbody>
</table>


<p>Bilgilerinize.</p>
<p>İyi &ccedil;alışmalar.</p>